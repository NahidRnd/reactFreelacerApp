
function DashboardTitle({title, subTitle}) {
  return (
    <div>
      <div className="mb-8">
      <h2 className="text--header">{title}</h2>
      <p>{subTitle}</p>
    </div>
    </div>
  )
}

export default DashboardTitle
